# Personal config — never committed (contains workspace/user IDs)
config.json
**/config.json

# OS / editor cruft
.DS_Store

# SDD scratch tooling (subagent-driven-development progress/briefs/reports)
.superpowers/
